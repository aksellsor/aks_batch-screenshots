const WAIT_FOR = 0; // General wait time (including delay before scrolling or for lazy-loaded content)
const WAIT_FOR_LAZYLOADED_CONTENT = true; // Boolean to enable or disable waiting for lazy-loaded content
const DELAY_BETWEEN_SCROLL = 100; // Delay (ms) before each scroll
const VIEWPORT_WIDTH = 1920; // Width of the viewport
const VIEWPORT_HEIGHT = 1080; // Height of the viewport
const REMOVE_POSITION_FIXED = true;

export {
  WAIT_FOR,
  WAIT_FOR_LAZYLOADED_CONTENT,
  DELAY_BETWEEN_SCROLL,
  VIEWPORT_WIDTH,
  VIEWPORT_HEIGHT,
  REMOVE_POSITION_FIXED,
};
